import React from 'react'
import PrintPokemon2 from './PokePic'
import {Link} from 'react-router-dom'

const PrintPokemon = props => {
    
    return(
       <div>
        {/* <Link to = {`/${props.printThis.name}`}>
        
           <h2>{props.printThis.map(element => <h2>{element.name}</h2>)}  
        </h2>
        </Link> */}
       </div>
        
    )
}
export default PrintPokemon